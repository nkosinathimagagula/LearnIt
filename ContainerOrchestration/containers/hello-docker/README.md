# hello-docker — Build & Run

This directory contains a simple Docker image. These instructions show how to build, run and test the image locally.

**Prerequisites:**
- Docker installed and running (Docker Desktop on Windows).

**Build the image**
Run from this directory:

```bash
docker build -t hello-docker .
```

**Run the container**

```bash
docker run hello-docker
```